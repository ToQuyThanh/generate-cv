import type { TemplateMeta } from '../types'

export const minimalMeta: TemplateMeta = {
  id: 'template_minimal_01',
  name: 'Minimal',
  isPremium: false,
  tags: ['minimal', 'clean', 'two-column'],
  defaultColor: '#059669',
}
